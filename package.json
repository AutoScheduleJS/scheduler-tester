{
  "name": "@autoschedule/scheduler-tester",
  "version": "0.0.0-development",
  "description": "Tool to test queries-scheduler",
  "main": "dist/index.bundle.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "ava": "ava **/*.test.js",
    "test": "tslint -p ./ && tsc && nyc --reporter=lcov npm run ava",
    "test:codecov": "tslint -p ./ && tsc && nyc npm run ava",
    "build": "webpack",
    "doc": "npm run build && typedoc --out docs/ dist/",
    "prepublish": "npm run build",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once",
    "watch:ts": "tsc -w",
    "watch:ava": "ava -w build/**/*.test.js",
    "watch:test": "concurrently -k -p \"[{name}]\" -n \"TypeScript,Ava\" -c \"blue.bold,magenta.bold\" \"npm run watch:ts\" \"npm run watch:ava\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AutoScheduleJS/scheduler-tester.git"
  },
  "private": false,
  "publishConfig": {
    "tag": "latest",
    "access": "public"
  },
  "keywords": [
    "state",
    "autoschedule"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/AutoScheduleJS/scheduler-tester/issues"
  },
  "homepage": "https://github.com/AutoScheduleJS/scheduler-tester#readme",
  "dependencies": {},
  "devDependencies": {
    "@autoschedule/queries-scheduler": "^3.0.0",
    "@semantic-release/release-notes-generator": "^6.0.3",
    "@types/ramda": "github:types/npm-ramda#dist",
    "ava": "^0.24.0",
    "concurrently": "^3.5.1",
    "cz-conventional-changelog": "^2.1.0",
    "nyc": "^11.4.1",
    "prettier": "^1.9.2",
    "ramda": "^0.25.0",
    "rxjs": "^5.5.6",
    "semantic-release": "^12.2.4",
    "travis-deploy-once": "^4.3.3",
    "ts-loader": "^3.3.1",
    "tslint": "^5.9.1",
    "tslint-config-prettier": "^1.6.0",
    "typedoc": "^0.9.0",
    "typescript": "^2.6.2",
    "webpack": "^3.10.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "release": {
    "publish": [
      "@semantic-release/npm",
      "@semantic-release/github"
    ]
  }
}
